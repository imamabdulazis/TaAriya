import * as React from 'react'
import { View } from "react-native"

const SpacerBotTop = ({ children }) => {
    return <View style={{ marginVertical: 7 }}>{children}</View>
}

export default SpacerBotTop;
