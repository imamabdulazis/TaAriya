import React from 'react';
import { Text, View } from 'react-native';

const Tentang = ({
    params,
}) => (
        <View>
            <Text>Tentang</Text>
        </View>
    );

export default Tentang;
